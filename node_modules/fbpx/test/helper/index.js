'use strict';

var ChixRenderer = require('../../renderer/chix');
var Parser = require('../../fbp');

exports.createParser = function() {
  var parser = new Parser();
  var renderer = new ChixRenderer();
  renderer.skipIDs = true;
  parser.addRenderer(renderer);
  return parser;
};